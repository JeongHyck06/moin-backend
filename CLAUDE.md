# Project Agent Rules

## Frontend

app 디렉터리 담당

프론트엔드 UI와 클라이언트 로직만 수정

백엔드 API 변경이 필요한 경우 임의로 backend를 수정하지 않고
필요한 API 스펙을 공유

## Backend

backend 디렉터리 담당

API, 데이터베이스, 서버 로직 수정

프론트엔드에 영향을 주는 API 응답 형식이 변경되면 변경 내용을 공유

## 주석

주석은 꼼꼼히 달되 한 줄 한 줄 설명하지 않음
왜 이렇게 했는지, 어떤 규칙을 구현한 것인지, 주의할 점을 적음
함수와 클래스 위에는 역할과 전제 조건을 한두 줄로
자명한 코드에는 주석을 달지 않음
AI가 쓴 것처럼 보이는 표현 금지: "이 함수는 ~를 수행합니다", "다음과 같이", 이모지, 과한 존댓말
개발자가 동료에게 남기는 메모처럼 짧고 구체적으로
마침표 사용 금지
이모지와 화살표 사용 금지
서술형 표현 사용 금지

## 검증

테스트는 핵심 로직에 하나만, 화면과 단순 CRUD 는 생략
실기동 검증과 스크린샷은 PM 이 요청할 때만
보고는 변경 파일 목록과 프론트가 알아야 할 것만 짧게

## Git

코드를 직접 구현하지 않음

Frontend와 Backend 작업 완료 후 Git 상태 확인

git status
git diff
git log

변경 내용을 기준으로 자연스럽게 커밋 분리

한 번에 몰아서 커밋하지 않음
파일별 또는 같은 목적의 작은 단위로 나눠서 여러 커밋으로 분리
의존성 파일(package.json과 lock, build.gradle)은 한 커밋
공통 컴포넌트와 설정 파일은 파일마다 한 커밋
화면은 화면마다 한 커밋

브랜치는 dev 하나만 사용
dev에서 개발하고 기능(Phase)마다 dev에서 main으로 PR을 만들고 PM이 바로 머지
머지는 사용자 확인 없이 자동으로 진행

커밋 형식

feat: 로그인 기능 구현
fix: 로그인 오류 수정
refactor: 인증 로직 개선
chore: 프로젝트 설정 변경

마침표 사용 금지
이모지와 화살표 사용 금지
서술형 표현 사용 금지

Co-authored-by 등 Claude 관련 trailer 추가 금지

현재 저장소에 설정된 user.name과 user.email 사용

PR 제목 형식

Feat: 로그인 기능 개발
Fix: 로그인 오류 수정
Refactor: 인증 로직 개선
